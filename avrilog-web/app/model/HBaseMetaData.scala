package model

object HBaseMetaData {
  object Account {
    val tableName = "account"
    val cfInfo = "info"
  }
  object Permission {
    val tableName = "permission"
    val cfInfo = "info"
  }
  object Application {
    val tableName = "application"
    val cfInfo = "info"
  }
}